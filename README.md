# Tests_with_Threads
<h3>Program using threads in Linux Ubuntu</h3><br>
Recieve an array by command line and calculates the average of its values, also show the minimum and maximum value in the array;
